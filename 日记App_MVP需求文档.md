# 手账日记 App — MVP 需求文档

## 项目概述

做一个网页版的个人手账日记应用，用户可以在浏览器里记录每天的生活，支持图文混排、心情标签、标签分类，数据存储在本地浏览器中。

技术栈要求：
- React（使用 Vite 创建项目）
- 数据存储：localStorage（不需要后端和数据库）
- 样式：Tailwind CSS
- 语言：中文界面

---

## 页面结构

共三个主要页面，通过底部导航栏切换：

1. 首页（日记列表）
2. 写日记页
3. 统计页

---

## 页面一：首页

### 顶部
- 标题"我的手账"
- 当前年月（如"2026年4月"）
- 右上角搜索按钮

### 日历条
- 横向滚动，显示当月所有日期
- 每个日期显示：星期缩写 + 日期数字
- 今天高亮显示（紫色圆形背景）
- 有日记记录的日期下方显示小绿点
- 点击日期，下方列表跳转到对应日期的日记

### 日记卡片列表
- 按时间倒序排列（最新的在最上面）
- 每张卡片包含：
  - 日期和时间（如"今天 · 10:32"）
  - 心情标签（如"😊 开心"）
  - 正文预览（最多显示3行，超出用...省略）
  - 图片缩略图（如果有图片，显示第一张，圆角矩形）
  - 标签列表（如"# 日常  # 朋友"）
- 点击卡片进入日记详情页

### 底部导航栏
- 首页 / 写日记 / 统计 三个 Tab
- 中间有一个突出的 + 圆形按钮，点击跳转到写日记页
- 当前激活的 Tab 图标和文字变为紫色

---

## 页面二：写日记页

### 顶部栏
- 左侧"← 返回"按钮
- 中间标题"写日记"
- 右侧"保存"按钮（点击保存并返回首页）

### 编辑区域
- 自动显示当前日期（如"2026年4月24日 · 星期四"），不可编辑
- 标题输入框（placeholder："给今天起个标题…"）
- 正文输入框（多行文本，placeholder："记录今天发生的事…"，可以自由输入）

### 心情选择
- 标题"今天的心情"
- 横排显示5个心情选项：
  - 😊 开心
  - 😌 平静
  - 😔 难过
  - 😤 烦躁
  - 🥰 幸福
- 点击选中，选中状态显示紫色边框和背景

### 标签
- 显示已添加的标签（如"# 日常"）
- 一个"+ 添加标签"按钮，点击弹出输入框，输入后回车确认
- 已添加的标签可以点击删除

### 图片上传
- 一个"📷 添加图片"按钮
- 点击触发文件选择（支持 jpg、png、gif）
- 选择后在编辑区下方显示图片预览，可以点击 × 删除

### 工具栏
- 底部固定工具栏，包含按钮：
  - 📷 图片（同上）
  - 📍 位置（点击后显示一个文字输入框，手动输入位置）
  - 🌤 天气（点击后显示选项：晴天 / 阴天 / 雨天 / 雪天）

---

## 页面三：统计页

### 顶部
- 标题"统计回顾"
- 副标题显示当前月份

### 数据卡片（2列网格）
- 本月记录天数
- 当前连续打卡天数
- 最常见心情（显示对应 emoji）
- 最常用标签

### 近7天记录量
- 横向柱状图
- 纵轴是日期（周一到周日）
- 横轴是当天的日记字数（或记录条数）
- 紫色填充

### 心情分布
- 标题"本月心情分布"
- 5个心情对应的柱状图，颜色各不同
- 下方显示 emoji 标签

---

## 日记详情页

从首页点击卡片进入，显示日记完整内容：
- 顶部显示"← 返回"和"编辑"按钮
- 显示完整标题、日期、心情
- 显示完整正文
- 显示图片（如果有）
- 显示标签、位置、天气信息
- 底部有"删除"按钮（点击后弹出确认对话框）

---

## 数据结构

每条日记存储为以下格式（JSON），存储在 localStorage 的 key `diary_entries` 中：

```json
{
  "id": "唯一ID（时间戳）",
  "title": "日记标题",
  "content": "正文内容",
  "mood": "happy | calm | sad | angry | love",
  "tags": ["日常", "朋友"],
  "images": ["base64格式的图片字符串"],
  "location": "位置文字",
  "weather": "sunny | cloudy | rainy | snowy",
  "createdAt": "2026-04-24T10:32:00.000Z",
  "updatedAt": "2026-04-24T10:32:00.000Z"
}
```

---

## 视觉风格

- 主色调：紫色（#7F77DD）
- 辅助色：绿色（#1D9E75，用于打卡/成功状态）
- 背景：白色，卡片有轻微边框
- 圆角：卡片使用较大圆角（12px）
- 字体：系统默认中文字体
- 风格：简洁清爽，像苹果备忘录一样轻量

---

## 开发顺序建议

1. 先搭好项目框架（Vite + React + Tailwind）
2. 实现 localStorage 的读写工具函数
3. 做写日记页（先能写、能保存）
4. 做首页（能看到列表）
5. 做详情页（能看、能删除）
6. 做统计页
7. 加日历条交互
8. 加图片上传功能

---

## 给 Claude Code 的启动指令

```
帮我用 React + Vite + Tailwind CSS 开发一个网页版手账日记应用。

请按照以下需求文档实现 MVP 版本，数据全部存储在 localStorage，不需要后端。

需求文档已在上方，请从第一步开始：初始化项目，安装依赖，然后按"开发顺序建议"逐步实现各个页面。

每完成一个模块请告诉我，我会在浏览器里查看效果后再继续。
```
